#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "lib_lista_chave.h"
#define stringingsize 256

char *codifica_string(lista_c_t *l, char *string, char *string_codificada){
    int i, tam;
    char *chave;

    char = malloc(sizeof(char)*2);

    tam = strlen(string);
    for(i = 0; i < tam; i++){
        *chave = *string[0];
        if (existe_chave(l, chave)){
            
        }
    }

    return string_codificada;
}
